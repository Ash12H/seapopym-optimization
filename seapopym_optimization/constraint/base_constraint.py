"""All the constraints (as penalty functions) used by the DEAP library to contraint parameters initialization."""

from __future__ import annotations

# Note: AbstractConstraint ABC removed in favor of Protocol-based approach.
# See seapopym_optimization.protocols for ConstraintProtocol.
